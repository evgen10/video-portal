import { CourceSortPipe } from './cource-sort.pipe';

describe('CourceSortPipe', () => {
  it('create an instance', () => {
    const pipe = new CourceSortPipe();
    expect(pipe).toBeTruthy();
  });
});
