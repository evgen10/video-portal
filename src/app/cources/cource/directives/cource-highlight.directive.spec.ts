import { CourceHighlightDirective } from './cource-highlight.directive';

describe('CourceHighlightDirective', () => {
  it('should create an instance', () => {
    // const directive = new CourceHighlightDirective();
    // expect(directive).toBeTruthy();
  });
});
